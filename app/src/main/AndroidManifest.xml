<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xuanyi.rxt">

    <!-- android:theme="@style/AppTheme" -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".application.RXTApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        >
        <activity
            android:name=".SplashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".Way0FastActivity" />
        <activity android:name=".Card0FastActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".Way5BankTelActivity" />
        <activity android:name=".CardShowActivity" />
        <activity android:name=".BankNormalActivity" />
        <activity android:name=".Card1GoldActivity" />
        <activity android:name=".Card2TripActivity" />
        <activity android:name=".Card3ShoppingActivity" />
        <activity android:name=".Way1NetLoanActivity" />
        <activity android:name=".Way2LatestCutActivity" />
        <activity android:name=".Way3QueryProgressActivity"></activity>
    </application>

</manifest>